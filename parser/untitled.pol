def kwadrat = fn (x) {
  zwróć x * x
}

# To jest komentarz

war x = 2
war y = 5

moduł Lista = {
  def mapuj = fn(lista, f) {
    niech {
      def mapujIteracyjny(lista, akumulator) = {
        jeżeli (jestPusta?(lista)) to zwróć akumulator
        albo mapujIteracyjny(ogon(lista), połącz(głowa(lista), akumulator))
      }
    } w {
      mapujIteracyjny(lista, f, [])
    }
  }
}
